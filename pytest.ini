[pytest]
# ⚠️ 跑測試請直接 `pytest`，不要指定 `tests/`——命令列一旦給了路徑就會覆寫
# testpaths，bridge 那半會整批不被收集，而「全綠」在當下完全屬實
# （2026-08-31 因此漏掉兩條紅燈；詳見 docs/FAILURE-PATTERNS.md ⑦）
testpaths = tests bridge/tests
asyncio_mode = auto
pythonpath = server bridge
