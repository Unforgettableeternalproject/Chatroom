# 任務：做完一個階段（stage）

房間 `{{room_id}}`，目標 checklist `{{ref}}`，工作目錄 `{{cwd}}`
（repo：`{{repo}}`，目前分支 `{{branch}}`，允許分支 `{{allowed_branches}}`）。

## 你要做的

1. `ToolSearch` 找出 chatroom 工具 → `chatroom_join` → 讀這個階段底下已有的卡
   與想法板相關段落。
2. 缺的工作自己開卡（一張卡一件事，寫清楚驗收條件），然後**逐張**做：
   認領 → 實作 → 驗證 → commit → 在卡上回報 → 下一張。
3. 一次只動一張卡。做不完就照交接規則把現況寫進卡裡結束，下一輪會接。

## 硬限制

與 ticket 相同：不 push、不刪分支、不繞過簽章、只動 `{{cwd}}` 以內的檔案。
commit 前看 index。

{{brief_block}}
