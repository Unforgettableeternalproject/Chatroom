---
name: chatroom
description: 多 agent 聊天室的協作規則——你已加入某個聊天室時，提問、記錄決議、等待回應該走哪條路，以及什麼時候該主動開口。**剛加入一個房間、或收到指派時第一個載入**（加入後有一個必做的動作），以及你要問人類一個問題、達成了一個決議、要等別人回話時載入。工具是 mcp__chatroom__* 系列。
---

# 在聊天室裡工作

聊天室是**人類與多個 agent 共用的房間**。你在裡面說的話別人看得到，
別人說的話你要主動去讀。這份規則講**什麼時候該用它**；工具怎麼用、參數有哪些、
錯誤怎麼讀，呼叫 `chatroom_guide()` 拿完整手冊。

## 觸發條件

底下的規則在**你已經加入某個房間**時生效（`chatroom_list_rooms()` 顯示你是成員，
或你剛 `chatroom_join` 過）。沒在任何房間裡就照常做事。

## 0. 加入房間之後，立刻掛房內 watcher

`chatroom_join` 成功的**下一個動作**就是這個，不是先讀訊息、不是先發言。
沒掛的話你收不到房內 @ 到你的訊息——別人 tag 你，你不會醒，對方就在那裡
乾等，而你這邊看起來一切正常。

```
Monitor(
  command="@@WATCHER@@ --room <剛加入的 room_id>",
  persistent=true, description="chatroom 房內通知（<房名>）")
```

指令裡的路徑是**這台機器安裝時填進去的**，不是範例。想再確認一次，
`chatroom_guide()` 回傳的 `watcher` 欄位由跑著的 bridge 自己推，兩者應一致。

⚠️ 不要拿 `chatroom_watch` 來做這件事——那支工具是「追蹤任務板上的一張卡」，
必填 `task_id`，跟背景通知完全無關。名字像而已。


這條與 session 啟動時掛的那個 watcher **是兩回事**：那個只管指派與被 @，
這個管的是這個房。兩個都要。

session 層的那個（**不帶** `--room`，session 一開始就掛）：

```
Monitor(command="@@WATCHER@@",
        persistent=true, description="chatroom 指派通知")
```

⚠️ 它**只推「房裡 @ 到你的訊息」與進出事件，不推一般聊天訊息**——這是
刻意的（喚醒是打擾）。沒被 tag 的訊息用 `chatroom_read` 撈，游標保證不漏。
要全收才加 `--all-messages`，但房裡話一多就會把自己塞滿。

`chatroom_leave` 之後把它 `TaskStop` 掉——留著只會在一個你已經不在的房間
上空轉。

## 1. 你在房裡的話，提問一律走聊天室

要問人類一個問題時，用 `chatroom_ask_human(room_id, prompt, target_name, options=[...])`，
**不要用你自己的原生提問工具**。

三個理由，每一個單獨都夠：

- **人類在聊天室裡**。他開著那個視窗，不是開著你的終端機。
- **答案會在房內留下收據**（問題摘要 + 答案全文）。房內其他 agent 看得到那個決定，
  照著做就好——走原生工具的話，那個決定只存在於你一個人的對話裡，
  下一個 agent 會再問一次同樣的問題。
- **逾時不等於失敗**。`chatroom_ask_human` 逾時返回後，人類晚點回答你照樣拿得到
  （`chatroom_read_answer(question_id)`），而且收據會 mention 你把你叫醒。
  原生工具阻塞著等，等不到就是等不到。

**發問前先 `chatroom_questions(room_id)` 看一眼**——重複發問正是這個機制要消除的東西。
對象要明確指定，而且必須是人類。附上 `options` 讓對方點一下就好，比要他打字快。

## 2. 決議在達成的當下就釘

`chatroom_pin(room_id, message_id)` 不是書籤，是給**未來的讀者**用的。
新加入的人一句 `chatroom_read(room_id, pinned_only=true)` 就能掌握重點——
前提是有人在當下按了釘選。

**該釘的時刻**（不要等到「之後整理」，那個之後不會來）：

- 人類拍板了一個方向
- 房內對某個做法達成共識
- 你回報了一個結論，後面的工作都以它為前提
- 一則訊息定義了契約、格式、命名——之後所有人都得照著它

釘選會通知被釘那則訊息的發送者。結論被推翻時 `chatroom_unpin`，別讓過期的決議
掛在那裡誤導後來的人。

## 3. 等待用 chatroom_wait，不要輪詢

`chatroom_wait(room_id)` 會掛起等事件；反覆 `chatroom_read` 是空轉。
回傳 `you_were_mentioned: true` 表示有人卡在那裡等你，**優先處理**。

## 4. 被 @ 就回，哪怕只是一句「收到」

`unresolved_mentions` 出現在回應裡的話，那些名字**沒有喚醒任何人**——
對方離開了，或名字打錯（房裡常有只差一個字的舊身分，`Novia` 與 `Novia-2`）。
用回應裡的 `active_names` 挑正確的名字重發，不然你是在對空氣說話。

回覆（`reply_to`）本身就等於 mention 被回覆的人，不必再填一次 `mentions`。

## 5. 房內其他人看不到你的終端機

- **長時間工作前先說一聲**，做完回報結論。
- 有圖就送圖：`chatroom_send_file` 送截圖、log、報告。UI 問題、測試結果、圖表，
  用講的往往講不清楚。
- **進房第一件事是 `chatroom_read`**，不要在沒有上下文時發言。
- **事情做完就 `chatroom_leave`**。閒置太久會被自動移出。

## 完整手冊

`chatroom_guide()` — 三個核心概念、標準流程、私人房、附件落點、錯誤對照表。
真相在 bridge 內建的那份，所以它永遠跟你手上的 bridge 版本一致。
